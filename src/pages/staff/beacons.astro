---
export const prerender = false
// Latarnicy is a section of the console now, so the old standalone page keeps
// working as a bookmark instead of being a second, drifting surface.
return Astro.redirect("/staff/?tab=beacons", 302)
---
